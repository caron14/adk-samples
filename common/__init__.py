# Import public functions/classes
from common.util import load_env

# Import setup_vertexai only if it's not causing circular imports
# We'll do the import in code where it's needed instead
# from common.vertex_setup import setup_vertexai
